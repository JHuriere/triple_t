name: settings_data
description: "A Flutter package for the data layer of the settings feature."
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  # Local packages
  tt_database:

  hooks_riverpod:

  freezed_annotation:
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

  build_runner:

  freezed:
  json_serializable:

flutter:
  generate: true
