name: game_domain
description: "A Flutter package for the domain layer of the game feature."
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  hooks_riverpod:
  riverpod_annotation:

  freezed_annotation:

  # Local packages

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito:

  # Local packages
  tt_linter:

  build_runner:
  riverpod_generator:
  freezed:

flutter:
  generate: true
