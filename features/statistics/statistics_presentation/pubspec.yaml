name: statistics_presentation
description: "A Flutter package for the presentation layer of the statistics feature."
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Local packages
  tt_i18n:
  user_domain:

  hooks_riverpod:
  flutter_hooks:
  riverpod_annotation:

  go_router:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Local packages
  tt_linter:

  build_runner:
  riverpod_generator:

  mockito:

flutter:
  generate: true
