name: tt_database
description: "A Flutter package for database management using Sembast."
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  sembast:
  sembast_web:
  path_provider:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Local packages
  tt_linter:

  build_runner:

  mockito:

flutter:
  generate: true
